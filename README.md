# ⚡️ SD-Transpiler

Профессиональный инструмент для конвертации человеческого описания в
структурированные промпты Stable Diffusion.
Работает на базе **Gemini 2.0 Flash**. Никаких лишних файлов, никаких утечек
ключей в Git, полная интеграция с системой.

## 🔥 Возможности

* **Реактивная скорость**: Использует модель `gemini-2.0-flash` (ответ < 1
  сек).
* **System Native Config**: API-ключ хранится в реестре Windows (`QSettings`),
  а не в валяющемся рядом `.env` файле.
* **Монолитность**: Все ресурсы (иконки, стили) зашиты внутрь. EXE-файл
  полностью самодостаточен.
* **Стилизация**: Два жестких пресета — `Anime` и `Photorealism`. Ничего
  лишнего.
* **Smart UI**: Анимированный фидбек копирования, защита от "подвисаний" UI (
  потоки), темная тема без вырвиглазных цветов.

---

## 🛠 Установка (Dev Mode)

Если ты хочешь ковырять код:

1. **Клонируй репо:**
   ```powershell
   git clone [https://github.com/YourUser/SD-Transpiler.git](https://github.com/YourUser/SD-Transpiler.git)
   cd SD-Transpiler
   ```

2. **Создай окружение (обязательно):**
   ```powershell
   python -m venv .venv
   .\.venv\Scripts\activate
   ```

3. **Поставь зависимости:**
   ```powershell
   pip install -r requirements.txt
   ```

4. **Запуск:**
   ```powershell
   python src/main.py
   ```

---

## 📦 Сборка в EXE (Release)

**НЕ ИСПОЛЬЗУЙ** `pyinstaller` напрямую, если не хочешь получить сломанные пути
импортов. Используй подготовленный скрипт, который создает временный лаунчер
для корректной упаковки пакета `src`.

1. Открой PowerShell в корне проекта.
2. Запусти скрипт сборки:
   ```powershell
   .\build.ps1
   ```
3. Готовый файл будет лежать в папке `dist/SD-Transpiler.exe`.

---

## 🔑 Конфигурация

При первом запуске программа сама попросит ввести **Gemini API Key**.

* Ключ сохраняется в: `HKEY_CURRENT_USER\Software\SD-Transpiler` (на Windows).
* Чтобы сбросить ключ, просто удали эту ветку реестра или используй `regedit`.

---

## 📂 Структура проекта

* `src/core/` — Логика генератора и работа с LLM.
* `src/ui/` — Интерфейс на PyQt5 (без .ui файлов, чистый код).
* `src/data/` — JSON-конфиги стилей и тегов качества.
* `build.ps1` — **Сборка проекта.**

---

## ⚠️ Known Issues

* Если антивирус ругается на EXE — это норма для неподписанных
  PyInstaller-сборок. Добавь в исключения.
* Для работы требуется подключение к интернету (Google API).

